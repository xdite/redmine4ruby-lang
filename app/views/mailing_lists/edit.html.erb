<h2><%= l(:label_mailing_list)%></h2>
<% labelled_tabular_form_for :mailng_list, @mailing_list, :url => {:action => 'edit' } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag l(:button_save) %>
<% end %>

<%= render :partial => 'trackings' %>
